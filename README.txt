Each directory contains the code to calculate each parameter: a)QUILLO, b)QUILLO2, c)other parameters (compactness, system temperature, surface area per residue, secondary structure composition).
Addittionally, there is code available for the calculation of deviations from expected values.
